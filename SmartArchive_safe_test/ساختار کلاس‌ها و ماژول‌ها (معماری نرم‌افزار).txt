┌─────────────────────────────────────────────────────────────────────┐
│                        main.py (ورودی اصلی)                       │
├─────────────────────────────────────────────────────────────────────┤
│  ├── Translator                     ← ترجمه دو زبانه (فارسی/انگلیسی) │
│  ├── DatabaseManager                ← مدیریت پایگاه داده SQLite     │
│  ├── ImageContentDetector           ← تشخیص محتوای تصاویر          │
│  │   ├── _load_model()              ← بارگذاری ResNet50 (اختیاری)  │
│  │   ├── _get_top5_labels()         ← دریافت ۵ برچسب برتر ResNet50 │
│  │   └── detect_content()           ← تشخیص نهایی محتوا            │
│  ├── ClassificationEngine           ← موتور اصلی دسته‌بندی         │
│  │   ├── EXTENSION_MAP              ← نگاشت پسوندها به دسته‌ها     │
│  │   ├── _calculate_hash()          ← محاسبه هش برای تشخیص تکراری  │
│  │   ├── _get_category()            ← تعیین دسته‌بندی فایل         │
│  │   ├── _get_destination_path()    ← تعیین مسیر مقصد              │
│  │   └── classify_and_copy()        ← اجرای دسته‌بندی و کپی        │
│  ├── ClassificationWorker (QThread) ← ترد پردازش (برای عدم قفل UI) │
│  └── MainWindow (QMainWindow)       ← پنجره اصلی برنامه            │
│      ├── setup_ui()                 ← ساخت رابط کاربری             │
│      ├── create_search_tab()        ← تب جستجو (اختیاری)           │
│      ├── create_duplicates_tab()    ← تب تکراری‌ها (اختیاری)       │
│      ├── start_classification()     ← شروع دسته‌بندی              │
│      ├── toggle_pause()             ← مکث/ادامه                    │
│      ├── stop_classification()      ← توقف                        │
│      ├── generate_report()          ← تولید گزارش                  │
│      └── apply_theme()              ← اعمال تم رنگی               │
├─────────────────────────────────────────────────────────────────────┤
│                    ai/image_analyzer.py                            │
├─────────────────────────────────────────────────────────────────────┤
│  └── ImageAnalyzer                   ← تحلیل تصاویر با ResNet50    │
│      ├── MATERIAL_KEYWORDS           ← کلمات کلیدی متریال          │
│      ├── FOOD_KEYWORDS              ← کلمات کلیدی غذا             │
│      ├── BUILDING_KEYWORDS          ← کلمات کلیدی ساختمان         │
│      ├── VEHICLE_KEYWORDS           ← کلمات کلیدی خودرو           │
│      ├── FAMILY_MAP                 ← خانواده‌های برچسب‌ها        │
│      └── get_best_category()        ← انتخاب بهترین دسته از Top-5 │
├─────────────────────────────────────────────────────────────────────┤
│                    ai/text_analyzer.py                             │
├─────────────────────────────────────────────────────────────────────┤
│  └── TextAnalyzer                    ← تحلیل اسناد متنی            │
│      ├── DOC_TYPES                  ← دیکشنری کلمات کلیدی اسناد   │
│      └── classify()                 ← تشخیص نوع سند از متن         │
└─────────────────────────────────────────────────────────────────────┘