۱. کاربر پوشه مبدا و مقصد را انتخاب می‌کند.
         ↓
۲. روی دکمه‌ی "شروع دسته‌بندی" کلیک می‌کند.
         ↓
۳. ClassificationWorker در ترد جداگانه شروع به کار می‌کند.
         ↓
۴. برای هر فایل:
   ├── ۴-۱. هش فایل محاسبه می‌شود (تشخیص تکراری).
   ├── ۴-۲. دسته‌بندی بر اساس پسوند یا محتوا انجام می‌شود.
   │   ├── اگر تصویر باشد → ImageContentDetector + ImageAnalyzer
   │   ├── اگر سند متنی باشد → TextAnalyzer
   │   └── اگر فایل طراحی باشد → بر اساس پسوند
   ├── ۴-۳. مسیر مقصد مشخص می‌شود.
   ├── ۴-۴. فایل کپی می‌شود.
   └── ۴-۵. اطلاعات در دیتابیس ذخیره می‌شود.
         ↓
۵. پیشرفت در UI نمایش داده می‌شود.
         ↓
۶. پس از اتمام، گزارش تولید می‌شود.